# Get source code
git clone https://github.com/xiph/vorbis
cd vorbis
git checkout v1.3.6

# Compile
mkdir -p build
cd build
cmake -DBUILD_SHARED_LIBS=OFF ..
make -j6

# Static libs are in:
./build/lib/libvorbis.a
./build/lib/libvorbisenc.a
./build/lib/libvorbisfile.a
