#ifndef TEXTFORMAT_H
#define TEXTFORMAT_H

enum class WrapMode { Wrap, Overflow, Hide };
enum class HorizontalAlignment { Left = 0, Center, Right };
enum class VerticalAlignment   { Top  = 0, Center, Bot   };

#endif // TEXTFORMAT_H
